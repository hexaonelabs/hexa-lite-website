---
import '@fontsource-variable/inter';

// Supports weights 100-900
import '@fontsource-variable/outfit';

// 'DM Sans'
// Nunito
// Dosis
// Outfit
// Roboto
// Literata
// 'IBM Plex Sans'
// Karla
// Poppins
// 'Fira Sans'
// 'Libre Franklin'
// Inconsolata
// Raleway
// Oswald
// 'Space Grotesk'
// Urbanist

---

<style is:inline>
  :root {
    --aw-font-sans: 'Outfit Variable';
    --aw-font-serif: 'Outfit Variable';
    --aw-font-heading: 'Outfit Variable';

    --aw-color-primary: rgb(0 144 255);
    --aw-color-primary-rgb: 0, 144, 255;
    --aw-color-secondary: rgb(76 204 230);
    --aw-color-secondary-rgb: 76, 204, 230;
    --aw-color-accent: linear-gradient(270deg, var(--aw-color-secondary),var(--aw-color-primary));

    --aw-color-text-heading: 'Outfit Variable';
    --aw-color-text-default: rgb(24, 13, 104);
    --aw-color-text-muted: rgb(16 16 16 / 66%);
    --aw-color-bg-page: rgb( 245,248,255);

    --aw-color-bg-page-dark: rgb(24, 36, 73);
    --aw-color-bg-page-shader: #eaf2ff;
    --background-gradient: #e6eefe;

    ::selection {
      background-color: lavender;
    }
  }

  .dark {
    --aw-font-sans: 'Outfit Variable';
    --aw-font-serif: 'Outfit Variable';
    --aw-font-heading: 'Outfit Variable';

    --aw-color-text-heading: rgb(247, 248, 248);
    --aw-color-text-default: rgb(229 236 246);
    --aw-color-text-muted: rgb(229 236 246 / 66%);
    --aw-color-bg-page: rgb(24, 36, 73);
    --aw-color-bg-page-shader: rgb(24, 36, 73);
    --aw-background-gradient: linear-gradient(125deg,#182449, rgba(61, 158, 255, 0.2));

    ::selection {
      background-color: black;
      color: snow;
    }
  }

  .text-accent {
    color: linear-gradient(270deg, var(--aw-color-secondary),var(--aw-color-primary));
    background: linear-gradient(270deg, var(--aw-color-secondary),var(--aw-color-primary));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: #0000;
  }

  h1 .text-accent {
    font-size: 4.4rem;
    list-style: 5rem;
  }

  html, body {
    font-size: 20px;
  }

  .gradientBg {
    background: var(--aw-background-gradient);
  }
  #onboard-on-defi {
    border-top: solid 1px rgba(var(--aw-color-primary-rgb), 0.5);
    box-shadow: 0px -60px 60px 0px rgba(var(--aw-color-primary-rgb), 0.15);
  }

  .shader__section,
  .bg__shader,
  footer {
    background: var(--aw-color-bg-page-shader);
  }
 
  h3.underlinedColor {
    position: relative;
    display: inline-block;
  }
  h3.underlinedColor:after {
    content: "";
    position: absolute;
    bottom: -0.8rem;
    left: 0;
    width: 280px;
    height: 3px;
    background: var(--aw-color-accent);
    border-radius: 50px;
  }

  #download {
    padding: 8rem 0;
  }

  .onboading__image {
    min-width: 280px;
    margin: 0px;
    border-radius: 32px;
    overflow: hidden;
    border: solid 2px var(--aw-color-primary);
    margin: auto;
  }

  .fitContent {
    object-fit: unset!important;
    max-width: fit-content;
  }

  .githubImg {
    max-width: 256px;
    margin: auto;
    fill: var(--aw-color-text-default);
  }
  .swap__image {
    transform: scale(1.6);
  }
  .liquid_staking__image {
    object-fit: contain!important;
    /* transform: scale(1.3); */

  }
  .liquid_staking__image::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--aw-color-accent);
    border-radius: 32px;
    z-index: -1;
  }
  .buy_with_fiat__image {
    max-width: 280px;
  }

  html.dark footer,
  html.dark #partners {
    background: rgba(0,0,0,0.2);
  }

  img.app__preview {
    padding: 0 1rem;
    transform: scale(1.5) translateX(3rem);
    filter: drop-shadow(0px 0px 60px rgba(var(--aw-color-primary-rgb),0.2)) brightness(1.1) contrast(1.1);
  }
  img.object-contain {
    object-fit: contain!important;
  }

  img.filter-drop-shadow {
    filter: drop-shadow(0px 0px 60px rgba(var(--aw-color-primary-rgb),0.2)) brightness(1.1) contrast(1.1)
  }
  .aw-link-active {
    color: var(--aw-color-primary);
  }

  .relative.m-auto.max-w-5xl.md\:pt-16.pt-10:after {
    content: '';
    position: absolute;
    display: block;
    bottom: 0;
    width: 100%;
    height: 130px;
    background: transparent;
    z-index: -1;
    margin-bottom: -60px;
    box-shadow: 0px -20px 20px -30px #000;
  }

  @media screen and (max-width: 1023px) {
    img.app__preview {
      transform: scale(1.1);
    }
  }

  section {
    overflow: hidden;
  }

  .accordion.active {
    background: rgba(var(--aw-color-primary-rgb), 0.2);
  }

</style>